[gcode_macro PARK]
description: Parks the toolhead (retract, Z-hop, move); no-ops while already paused.
gcode:
  {% if not printer.pause_resume.is_paused %}
    _TOOLHEAD_PARK_PAUSE_CANCEL {rawparams}
  {% endif %}

[gcode_macro M125]
description: Alias for PARK.
gcode:
    PARK
