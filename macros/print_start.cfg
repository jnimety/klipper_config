#####################################################################
#   Based on "A better print_start macro for v0"
#   https://github.com/jontek2/A-better-print_start-macro
#####################################################################

[gcode_macro PRINT_START]
gcode:
  # Fetches data from your slicer. Such as bed temp, extruder temp, chamber temp and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default("45")|int %}
  {% set target_chamber_minimal = params.CHAMBER_MINIMAL|default("0")|int %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}

  # Status indicator (printer-specific; no-op where not supported)
  PRINT_START_NOTIFY MSG="PRINT_START initiated."

  # Clear any lingering pause states
  CLEAR_PAUSE

  # Homes the printer and sets absolute positioning
  G28                                                 # Full home (XYZ)
  G90                                                 # Absolute position

  # Heat the bed, then soak until the chamber is up to temp (printer-specific)
  G1 X{x_wait} Y{y_wait} Z15 F9000                    # Go to center of the bed
  M190 S{target_bed}                                  # Set target temp for the bed
  HEAT_SOAK CHAMBER={target_chamber} CHAMBER_MINIMAL={target_chamber_minimal}

  # Re-home Z now that the frame has reached temperature (thermal expansion during
  # the soak can shift the Z endstop reference established by the cold homing above)
  G28 Z

  # Check bed level while hot (printer-specific; no-op where not supported)
  CHECK_BED_LEVEL

  # Load a saved bed mesh profile (printer-specific; no-op where not supported)
  LOAD_BED_MESH

  # Heats up the hotend up to target via slicer
  M107                                                # Turn off partcooling fan
  M109 S{target_extruder}                             # Heat hotend to print temp

  # Homing/heating/leveling are done — the sliced print is actually starting now.
  # Also re-asserts the blue "actively running" indicator, since HEAT_SOAK_NOTIFY above
  # may have left it on orange for the whole print otherwise.
  PRINT_START_NOTIFY MSG="Printing started."

  # Create a purge line and starts the print
  SQUIGGLY_PURGE

[gcode_macro CHECK_BED_LEVEL]
description: Default no-op — override per printer if bed-level checking is supported.
gcode:
  # nothing to check

[gcode_macro LOAD_BED_MESH]
description: Default no-op — override per printer if a [bed_mesh] profile can be loaded.
gcode:
  # nothing to load
